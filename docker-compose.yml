services:
  personal-blog:
    image: nginx:1.27-alpine
    container_name: personal-blog
    restart: unless-stopped
    ports:
      - "${BLOG_HOST_PORT:-80}:80"
    volumes:
      - /opt/personal-blog/out:/usr/share/nginx/html:ro
      - /opt/personal-blog/nginx:/etc/nginx/conf.d:ro

